# 更新日志

本项目遵循语义化版本，最新版本在最上方。

## v1.12.4

改进

- **配色整体换成 Google 简约浅色风**:白面板 + 浅灰分层(#f8f9fa/#f1f3f4)+ 谷歌蓝(#1a73e8)主色,文字用 Google 标准灰阶(#202124/#5f6368/#80868b),危险色换 #d93025;"默认"主题即谷歌蓝,其余主题(sage/ocean/lavender/rose/amber/mono)的 accent 全部换成浅底上可读的谷歌系饱和色,自动备份徽章绿色同步加深。
- 纯换色,布局/间距/动效沿用 v1.12.3 的精修结果。

## v1.12.3

改进

- **界面全面精修**(纯视觉,零结构与功能改动):
  - 底色加深一档、层级更分明,新增更淡的分隔线色,行间分隔不再生硬;
  - 分组标题改为**吸顶**(滚动时钉在列表顶部,行从其下方滚过),收起工具栏时也正确贴顶;
  - 标题行加主题色圆点标识;标签页激活下划线带滑入动效与柔光;
  - 搜索框/下拉聚焦时加主题色光环;复选框选中带弹入动效;主题下拉/编辑器同样式聚焦环;
  - 删除选中按钮启用时带危险色投影;行内图标按钮、状态栏、空状态、弹窗底部按钮统一打磨;
  - 键盘焦点环(:focus-visible)与文本选区颜色跟随主题;滚动条圆角化;
  - 窄屏/触屏行内操作按钮加大触控目标。
- **修复**:列表顶部留白改为占位块实现 — 原.padding 方案会让吸顶分组头被顶到留白下缘,工具栏收起后顶部留出一条空洞。

## v1.12.2

修复

- **滚动抖动彻底修复(v1.12.1 回归)**:顶部工具区改为绝对定位悬浮在列表上方(列表滚动内容顶部留等高空白),收起/展开只是 translateY 滑出滑入,完全不参与布局 — 条目位置与滚动位置彻底解耦,任何条目数量下都不会再抖动;列表不可滚动(条目太少)时收起不触发。交互恢复 v1.11.0 设计:下滚收起,到顶后下拉(移动端)/滚轮上滚(桌面端)展开。

## v1.12.1

修复

- **顶部收起不再"顶跳"条目**:顶部工具区(标题/标签页/搜索栏)收起量改为与滚动位置 1:1 绑定(负 margin 跟随 scrollTop 渐进变化),不再用 max-height 一次性压扁 — 向下滑动时条目不会再被突然推上去一大截,收起完全跟手;滚回顶部时自动逐帧展开,v1.11.0 的"二次下拉展开"手势随之取消(桌面滚轮/移动端触摸行为一致)。弹窗手动拉伸导致顶部换行变高时,收起量经 ResizeObserver 自动重新对齐。

## v1.12.0

新功能

- **回收站**:删除角色卡/预设/世界书/聊天等条目前自动备份到回收站,7 天内可还原,过期自动清理;回收站面板显示过期倒计时,支持单条还原/下载/删除。
- **批量改名**:工具栏新增批量改名按钮,支持加前缀、加后缀、查找替换(普通文本)、正则替换四种规则,带实时预览,非法字符自动跳过。
- **全局内容搜索**:工具栏新增搜索范围切换按钮(仅名称 / 名称+内容),启用后搜索框可匹配条目内联 JSON 内容(如角色卡描述、预设参数),不再只搜名称。
- **TauriTavern 增强绑定(可选)**:自动检测 TauriTavern 环境,回收站数据优先写入 `__TAURITAVERN__.api.extension.store`(跨设备可迁移),IndexedDB 作为 fallback,双写双读保持兼容。

## v1.11.2

兼容性修复(为合并上游准备)

- **聊天记录"全部模式"加 fallback**:旧版酒馆 `/api/chats/search` 必须传 `avatar_url`,全部模式 400 时自动 fallback 到第一个角色,不再报错。
- **世界书 read/write 加 fallback**:极老版本没有 `/api/worldinfo/get|edit` 接口时,自动 fallback 到前端 `ctx().loadWorldInfo / saveWorldInfo`。
- **预设默认展开组改用 apiId 判断**:不再硬编码中文 label,不受界面语言影响。
- **localStorage key 加命名空间**:`stdm_theme` → `stdm_data_manager_theme`,避免与其他扩展冲突。
- **图标按钮加文字 fallback 结构**:`<i>` 后带隐藏 `<span>`,FA 不可用时至少有结构可扩展。

## v1.11.1

修复

- **切换 tab 不再残留旧内容**:切 tab 瞬间清空旧列表并显示"加载中…"占位,不会再看到上一个 tab 的条目;同时顶部自动展开,方便直接用搜索框,滚动位置归零。

## v1.11.0

改进

- **顶部改为"二次拖动"展开**:向下滚动时顶部立即收起;滚到列表最顶部后顶部**不自动**出现,需要在顶部**再往下拉一次**(下拉手势,类似下拉刷新)才展开。桌面端对应:到顶后再滚轮向上。浏览条目时顶部彻底不占空间。

## v1.10.5

修复

- **分组折叠彻底修好**:改为就地切换(display:none),不再重建列表 DOM,任何分组(含聊天补全预设)都能正常展开/收起,不抽搐、不跳位置。
- **顶部展开加 250ms 延迟**:回到顶部后等 250ms 才展开,期间再往下滚会取消展开,不会突兀闪现。

## v1.10.4

修复

- **顶部只在拉到列表最顶部时才展开**:之前只要向上滑一点点就展开,反而干扰;现在逻辑改为"滚动位置 > 20px 收起,回到 ≤10px 才展开",浏览条目时顶部彻底消失。
- **展开分组不再弹回顶部**:renderList 渲染前记录 `scrollTop`,渲染完后恢复,展开/折叠分组时位置保持。
- **空预设分组不显示**:预设页加载后,没有任何条目的分组直接不渲染,不再显示"0 条"的空标题。

## v1.10.3

修复

- **预设分组点击抽搐**:去掉 `.stdm_group` 的 `position: sticky`(sticky 元素在 renderList 重建时会被瞬间拉回顶部再定位,导致视觉抽搐),改为普通标题。
- **预设分组点击无响应**:同上,sticky 重建覆盖点击事件的问题一并修复;现在所有分组(包括"聊天补全预设")都可点击折叠/展开。

改进

- **顶部收起/展开平滑动画**:header/tabs/toolbar 包进 `.stdm_top_wrap` 容器,用 `max-height 0.3s` 过渡,收起时不再瞬间消失,平滑向上滑出。

## v1.10.0

新增

- **上滑自动收起顶部**:在列表上向上滑,header / tabs / toolbar 全部一起向上收起,列表区域撑满整个面板;向下滑或回到顶部再展开。30px 阈值防抖动。
- **预设分组折叠**:预设页除"聊天补全预设"外其他分组默认折叠,点击分组标题可展开/收起,右侧显示条目数,界面不再冗杂。

改进

- 主题下拉去掉"Claude"等英文名,改为中文简短标签(默认 / 鼠尾草 / 海蓝 / 薰衣草 / 蔷薇 / 琥珀 / 单色)。

## v1.9.0

修复 + 重做

- **修掉"字竖起来"**:列表名字加 `white-space: nowrap; word-break: keep-all`,flex 容器补 `min-width: 0`,中文长名字正确一行显示并省略,不再一个字一行。
- **改 Claude 暗色风**:面板整体换成 Claude 那种温暖深棕 (#1c1917) + 陶土橙 accent (#d97757),所有主题(Claude / 鼠尾草 / 海蓝 / 薰衣草 / 蔷薇 / 琥珀 / 单色)共享同一棕底基调,只换 accent 颜色,看起来更统一专业。
- **聊天记录"全部"模式**:切换器第一项是「— 全部聊天 —」,列出**所有**角色的聊天记录(含没筛选到的杂项),再选具体角色时再按角色过滤。chat 条目显示格式改成 `角色名 · 文件名`,一眼看清是谁的聊天。
- **popup 默认尺寸回收**:99vw×98vh 改为 96vw×92vh,留出少量外边距,避免满屏时的压迫感。
- **popup-controls 样式收编**:底部"关闭"按钮也走主题,不再是酒馆默认样式。
- 群聊聊天不在本标签显示(TauriTavern 群聊走单独接口,需要的话告诉我再加)。

## v1.8.0

重做(视觉)

- **整体重写为现代扁平风(Linear / Vercel 风格)**:去掉花哨渐变与紫色霓虹,改为克制纯色 + 细腻边框;弹窗、工具栏、tab、按钮、列表、历史面板全部统一视觉语言。
- **行内操作按钮图标化**:改名 / 编辑 / 导出 / 删除改为图标按钮(hover 显 tooltip),不再占文字宽度;hover 行时才淡入,默认干净。
- **选中行视觉反馈**:勾选的行有左侧 accent 竖线 + 整行 accent 浅底,一眼看清当前选了哪些。
- **8 套主题全部重写配色**:星霜 Aurora / 暗玫瑰 Rosé / 深海 Teal / 琥珀 Amber / 暗夜 Midnight / 薄荷 Mint / 石墨 Graphite / 樱粉 Sakura,均按新扁平体系调好。
- **工具栏按钮图标化**:全选 / 清空 / 刷新改为图标;主按钮(删除选中)用高亮红,危险操作清晰。
- **分组标题 sticky**:滚动时当前分组标题钉在顶部,不再丢失上下文。

## v1.7.1

改进

- **条目两行布局**:每行的复选框/头像/名字/标签独立一行,「改名 / 编辑 / 导出 / 删除」按钮整组右对齐放在第二行,名字再长也不会被按钮挤掉。
- **面板默认尺寸拉满**:Popup 默认 `99vw × 98vh`,几乎全屏;仍可从右下角拖角缩小或放大。
- **再次压缩垂直留白**:header / toolbar / row / list 的 padding 再压一档,同屏可显示条目数进一步提升。

## v1.7.0

改进

- **面板可拖角缩放**:Popup 加了 `resize: both`,可从右下角拖动调整整个面板大小,一次性看到更多条目;默认拉满到 98vw × 94vh。
- **信息密度大幅提升**:header / tabs / toolbar / row / checkbox / 按钮的 padding 与字号统一压缩,行高更紧,删除前能在一屏内看到约 2 倍条目数。
- **新增 4 套主题**:暗夜 Midnight、薄荷 Mint、石墨 Graphite、樱粉 Sakura,加上原有 4 套共 8 套,主题切换器更醒目。
- **去掉突兀的 emoji**:标题、历史按钮、撤销按钮、设置抽屉、历史面板标题上的 🗂️ / 🕘 / ↩ 等 emoji 一并移除,纯文字呈现。

## v1.6.1

修复

- **TauriTavern 上世界书列表读不到**:TauriTavern 没有 `/api/worldinfo/list` 接口,且部分场景下 `settings.world_names` 为空导致 fallback 404。现在世界书列表**优先走前端 context 的 `getWorldInfoNames()`**(官方 ST 与 TauriTavern 都暴露),其次才用 `settings.world_names`,最后才是 list 接口,三条链路逐级兜底。

## v1.6.0

新增

- **背景图片管理**：新增「背景图片」标签页，支持多选批量删除、改名、导出、单条删除；删除前自动备份、可撤销、进历史。批量备份会打包成单个 zip（内含原图，可直接重新导入酒馆）。

修复

- **世界书 / 部分标签加载失败（`/api/worldinfo/list` 返回 404）**：较老版本酒馆没有该接口。现在世界书列表优先从 `settings` 的 `world_names` 读取（所有版本通用），彻底解决 404。

## v1.5.0

新增

- **用户设定 / 面具（Persona）管理**：新增标签页，支持多选批量删除、改名、导出、单条删除；删除走官方头像接口 + 前端设置对象（`saveSettingsDebounced`），不会被酒馆自动保存覆盖。
- **新建面具自动备份**：挂接酒馆的 persona 事件，每次新建面具时自动把它（头像 + 名字 + 描述）存入历史并提示。历史中带绿色「自动」标记，可下载或还原。开关在「扩展设置 → 数据管家」，默认开启。

## v1.4.0

新增

- **可切换配色主题**：面板顶部新增配色下拉，内置 4 套独立主题——星霜 Aurora、暗玫瑰 Rosé、深海 Teal、琥珀 Amber。配色不再跟随酒馆主题，选中即生效并记忆到本地。

## v1.3.0

新增

- **删除历史**：面板顶部「🕘 历史」按钮。每次删除的完整备份存入本地数据库（IndexedDB），可查看、下载、还原、删除；顶部可清空。不受「删除时下载备份」开关影响，最多保留最近 60 批。

## v1.2.1

修复

- 取消勾选「删除时下载备份」后，删除角色卡仍会下载备份（现在严格遵循开关）。
- 批量删除角色卡触发浏览器「允许下载多个文件」拦截、被中断后静默失败：现在一批备份打包成单个文件（zip / JSON），避开多文件拦截。
- 角色卡无法导出：改用官方 `/api/characters/export` 接口。

改进

- 备份失败时不再静默继续删除，而是跳过该项并提示。
- 角色卡支持撤销与从历史还原。

## v1.2.0

修复

- 手机端只显示半屏 / 背景透明：改用酒馆内置 Popup 系统承载面板，手机端全屏。

## v1.1.0

改进

- 首轮手机端适配：面板全屏、标签横滑、操作按钮触屏常显、控件窄屏重排。

## v1.0.0

首个版本

- 一个面板集中管理预设、世界书、角色卡、聊天记录、主题美化。
- 实时搜索、多选批量删除、改名、编辑 JSON、导出、删除前自动备份 + 一键撤销。
- 全部走官方后端接口，云端/服务器部署同样可用。
